# Проверки

## Локально

```sh
scripts/check-version.sh
scripts/validate.sh
tests/run.sh
```

Проверяются shell syntax, JSON, JavaScript syntax при наличии Node.js, ucode compile при наличии host tool, package contract, secret patterns и production scoring implementation.

Тесты мониторинга выполняют production `buildModel` (разрывы, серии ошибок, переходы, границы периода и пропуски сбора) и ucode adapter/collector/store (полный сбор измерения, вложенные live-группы, ошибки API, смена сервера во время пробы, ограничение истории). Backend-тест требует host `ucode`; при его отсутствии явно выводится SKIP.

## Проверки интерфейса и роутера

Для текущего интерфейса выполнены браузерные проверки светлой/тёмной темы и ширин 320–1680 px: график, подсказка около курсора, клавиатурная навигация, история, запуск/отмена, подтверждение выбора узла и экспорт на RPC-стенде. Вкладки настроек, Save/Reset и сохранение оформления проверены с настоящими модулями LuCI form/view. Эти проверки используют тестовые ответы API и не подтверждают переключение production-сервера или реальную загрузку Full.

На работающем OpenWrt 25.12.5 / Forkop 1.0.5 / sing-box 1.12.17 отдельно проверены фоновый сбор и RPC мониторинга, ограничение истории и сохранение имён при повторном использовании runtime tag. `tests/test_monitor.uc` также выполнен нативным ucode с изолированной UCI-фикстурой и подменёнными внешними командами. Чистая установка/удаление APK и весь цикл Full остаются отдельными интеграционными проверками ниже.

## SDK

Основная integration check выполняется `.github/workflows/build-openwrt-apk.yml` на official OpenWrt 25.12.4 SDK. Workflow проверяет SHA-256 SDK, pin Forkop, наличие двух APK, APK v3 metadata/dependencies через штатный `apk adbdump`, contents через `apk extract` и deterministic release names.

## Live OpenWrt checklist

Эти проверки нельзя считать выполненными без router/VM:

1. install обоих APK на чистом OpenWrt 25.12.4;
2. `ubus -v list forkop-analyzer` и ACL под LuCI session;
3. procd start/stop/restart;
4. Quick на активной subscription;
5. Gaming cancellation во время samples;
6. Full temporary port/process/config cleanup;
7. production selector до/после;
8. JSON/CSV export;
9. uninstall без изменений Forkop/sing-box.

## Выбор серверов

`tests/test_selection.sh` выполняет production-функцию запуска с изолированными внешними командами: старый лимит 15, ручной запуск всех 45, подмножество, положительный лимит, неверные аргументы, снятие блокировки при ошибке и соответствие плана счётчику. При наличии ucode дополнительно выполняется настоящий `adapter_cli.uc` с фикстурой списка: выбор tags, удаление дублей, отклонение пустого/неизвестного/невалидного списка.

На браузерном RPC-стенде проверены 45 серверов, поиск без потери выбора, выбор флажками и клавиатурой, смена группы, все три профиля, недостаточный бюджет и разовый лимит. Edge, ширины 320/390/900/1440/1680 px, светлая и тёмная темы: ошибок JavaScript и горизонтального переполнения нет. Это не проверка реального Full на роутере.

2026-09-13: текущие исходники прошли полный `scripts/validate.sh` через `ssh docker` во временном Alpine 3.22 контейнере с ucode 0.0.20250529: включая monitor backend, выбор tags и Linux-проверки прав scheduler, без SKIP. После точечной установки на GL-MT6000 проверены SHA-256 всех 7 файлов и выдача новых JS/CSS по HTTP, новые аргументы ubus `start`, 45 узлов в `nodes`, отклонение пустого списка и реальный Quick одного выбранного узла (1/1, 3/3 успешных проб). Monitor RPC работает, конфигурации Analyzer/Forkop сохранили хеши. Backup и rollback: `/root/forkop-analyzer-backups/selection-20260913/rollback.sh`. Полный замер скорости всех 45 серверов после установки не запускался.

## Отмена benchmark

Worker запускает длительные пробы через отдельную process group `timeout -k 2` и ожидает их прерываемым shell `wait`. При отмене останавливается вся группа пробы; временный sing-box получает TERM и затем KILL, если не завершился за 2 секунды. Проверка текущего selector при завершении также ограничена timeout. Уже завершённые результаты сохраняются.

`tests/test_cancellation.py` запускает настоящий worker с изолированными командами, зависающими на latency, prepare-isolated, sing-box check и download. Потомки игнорируют TERM: проверяются escalation, статус cancelled, отсутствие работающих потомков, удаление временных файлов и освобождение lock. На Alpine 3.22 через SSH Docker полный validate прошёл; отмена заняла 2.02 секунды на первых трёх этапах и 4.07 секунды на download вместе с зависшим sing-box.

2026-09-13 исправленный worker установлен на GL-MT6000. Реальная отмена Full во время активного curl завершилась за 1 секунду, status=cancelled, каталог задачи и lock удалены, хеши конфигураций Analyzer/Forkop не изменились. Предыдущий worker сохранён в `/root/forkop-analyzer-backups/cancel-20260913/worker`.

## Sites и выбор найденных серверов

Для обновления Sites 2026-09-13 выполнены Linux/ucode-проверки через SSH Docker, включая отмену Sites (4.07 секунды при игнорировании TERM), классификацию HTTP 2xx/3xx/403/429/404/405/5xx и curl-ошибок, частичные результаты и CSV. В браузерном RPC-стенде проверены кнопки выбора/снятия найденных, сохранение выбора вне фильтра, пустой поиск, запуск Sites, список 20 доменов и таблицы: 320/390/900/1440/1680 px, светлая/тёмная тема, без JS ошибок и горизонтального переполнения. Обновление установлено после завершения пользовательского Full 20/20. Все 14 хешей совпали, новые ресурсы отдаются по HTTP, monitor RPC работает, конфигурации Forkop/Analyzer не изменились. Реальный Sites на одном сервере завершился 1/1 с 20 результатами: 16 HTTP 2xx, 3 ограничения сайта, 1 HEAD не поддерживается. Временные файлы и lock удалены, пользовательский Full сохранён. Backup/rollback: `/root/forkop-analyzer-backups/sites-20260913/rollback.sh`. Фоновое ожидание отключено.

Флаги: проверены реальные SVG-изображения (`complete` и `naturalWidth`), 45 серверов в селекторе, динамическая замена текста, несколько флагов в строке, неизвестный код и текст с HTML-символами. Browser RPC-стенд: светлая/тёмная тема, 320/390/900/1440/1680 px, без ошибок и переполнения. SVG-файлы проверены на отсутствие script/foreignObject/внешних href; MIT attribution включён. Флаги установлены в составе пакета из 14 файлов.


Региональная проверка ChatGPT/Claude: GET с ограничением 64 КиБ, явное сообщение о регионе, Cloudflare challenge, скрипты с текстом ошибки, пустой HTML, curl 23 на границе и реальные ошибки передачи проверены в `tests/test_sites.py`. Остальные 17 доменов используют HEAD, Canva исключена. Полный `scripts/validate.sh` прошёл в Alpine/ucode; отмена Sites с игнорирующими TERM потомками завершилась за 4.07 секунды без оставшихся процессов. Browser RPC-стенд подтвердил чекбоксы 18×18 и отсутствие повторения одинаковых tag/display_name. Реальный Sites на роутере: 1/1 сервер, 19 сайтов, 16 HTTP 2xx, 2 Cloudflare challenge (ChatGPT/Claude с `cf-mitigated`), 1 HEAD unsupported. Все 15 файлов и исходные конфигурации проверены SHA-256. Backup: `/root/forkop-analyzer-backups/body-20260913/rollback.sh`.
